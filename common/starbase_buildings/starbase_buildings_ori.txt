originium_harvester = {
    icon = "GFX_starbase_nanite_harvester"
    construction_days = 1

    potential = {
        # exists = owner
        # owner = {
        #     has_tradition = tr_nanotech_adopt
        # }
        is_normal_starbase = yes
    }

    possible = {
        custom_tooltip = {
            fail_text = "requires_starport"
            has_starbase_size >= starbase_starport
        }

        custom_tooltip = {
            fail_text = requires_planetoids_for_originium
            solar_system = {
                any_system_planet = {
                    can_have_mineral_deposits = yes
                    NOT = { has_deposit = d_originium_harvester_deposit }
                }
            }
        }
    }

    resources = {
        category = starbase_buildings
        cost = {
            alloys = 1
        }
        upkeep = {
            energy = 4
            mult = value:originium_harvester_deposit_num
        }
    }

    on_finished = {
        solar_system = {
            system_event = {
                id = originium_production.5050
            }
        }
    }

    on_destroyed = {
        solar_system = {
            system_event = {
                id = originium.5051
            }
        }
    }

    custom_tooltip = originium_harvester_tooltip

    ai_weight = {
        weight = 100 #use same weight here - ai weighting is done in starbase_types.txt
        modifier = {}
    }
}